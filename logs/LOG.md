# Study Log — event layer (append-only)

> One dated entry per session. Never edited after writing. Keep struggles, dead ends, and confusion *in* — they're the most valuable data. Tags: `[STRUGGLE] [INSIGHT] [NEEDS_RECALL]`. New entries go at the **bottom**.

---

## Day 1 — 2026-06-19 — Python on-ramp + Big-O
Source: NeetCode (Arrays & Hashing). Two Sum (#1), Contains Duplicate (#217).
Hashmap/set for O(1) lookup [INSIGHT] — a dict kills the nested-loop scan. Slips: `range(x)` vs `range(len(x))`, and `nums.add` vs `seen.add` [STRUGGLE].

## Day 2 — 2026-06-20 — Frequency counting + group-by-key
Valid Anagram (#242), Group Anagrams (#49) — first Medium, reproduced clean from memory [INSIGHT]. Group by a canonical key (sorted string / char-count).

## Day 3 — 2026-06-20 — Bucket sort
Top K Frequent (#347) via bucket sort; derived time + space O(n) yourself [INSIGHT]. Got overloaded with Python idioms mid-session [STRUGGLE] → set the **pre-teach rule** (one new concept in isolation first). Product of Array Except Self deferred to Day 4.

## Day 4 — 2026-06-22 — Prefix/suffix + first spaced re-solves
Reproduced bucket sort cold (re-solve = method working). Product of Array Except Self (#238) via prefix/suffix products, incl. the O(1)-extra-space upgrade; derived complexity yourself. Slips: forgot `return` [STRUGGLE]; `for item in list` ordering.

## Day 5 — 2026-06-24 — Two Pointers
Taught `while`, `elif`. Palindrome + Two Sum II (#167, sorted). Reproduced the O(1) Product solution cold. Spotted that the basic palindrome fails on real input → extended to full Valid Palindrome (#125) with `.lower()`/`.isalnum()`. Slips: guessed O(1) space — it's O(n) when you build a cleaned string [STRUGGLE]; `.append[x]` brackets.

## Day 6 — 2026-06-26 — First interleaved review + greedy
Interleaved (unlabeled): Contains Duplicate, Two Sum, Product — all correct; correctly chose hashmap for *unsorted* Two Sum [INSIGHT]. Container With Most Water (#11) — nailed the "move the shorter wall" greedy insight yourself. Slips: skipped verbalizing the pattern before coding; used `i`/`j` instead of `left`/`right` [STRUGGLE].

## Day 7 — 2026-06-26 — Sliding Window (running-state), start of Week 2
Best Time to Buy/Sell Stock (#121). First attempt over-engineered (4 vars + time guards, buggy) → reframed to running-min + best-profit, 2 vars [INSIGHT]: keep the minimum necessary state. Verbalized patterns in warm-up ✅. Dropped the anagram length check, re-added [STRUGGLE].

## Day 8 — 2026-06-28 — Sliding Window (variable-size)
Longest Substring Without Repeating (#3) through self-driven iteration: clear-set (wrong) → shrink-from-left → +maxLen (correct). Taught **amortized analysis** (forward-only pointer ⇒ O(n), not O(n²)) [INSIGHT]. Slips: carried `k log k` from Group Anagrams (no sort here) [STRUGGLE]; under-read the spec ("alphanumeric only"). Spaced review caught real decay on Valid Palindrome (had dropped cleaning + reintroduced an infinite loop) [NEEDS_RECALL] → reset to 1d. Good: correctly called Valid Palindrome O(n) space — the Day-5 correction stuck.

## Day 9 — 2026-06-29 — Binary Search (new) · #704, #74
Block 1 reviews: Longest Substring (#3) **PASS** with unprompted amortized O(n) reasoning [INSIGHT]; Valid Palindrome (#125) **FAIL** again — compared `s` instead of the cleaned string [STRUGGLE][NEEDS_RECALL] → reset to 1d (root cause: wrong-variable reference, **M-004 now 2×**). Binary Search (#704): inverted the discard direction first (**M-012**), fixed after reasoning *small→right / big→left*. Search a 2D Matrix (#74): correct first try via the **virtual-flattened index** (`mid//cols`, `mid%cols`), complexity stated unprompted [INSIGHT]. Hit the **2-new/day sprint pace**.

## Day 10 — 2026-06-30 — Binary search ON THE ANSWER · Koko #875
Block 1 (5 re-solves): #704, #74, #347, #121 all **PASS** clean; **#125 Valid Palindrome** — the recurring wrong-variable bug (M-004) **did NOT recur** (pre-empt worked ✅), but a fresh typo `.isalum`→`.isalnum` keeps it red [NEEDS_RECALL]. **Koko (#875):** learned to search the *answer space* — speed `[1, max(piles)]`, hours = Σ`ceil(p/k)`, monotonic. Took 3 tries on the boundary logic: returned only on `==` → `None` on an early finish; then returned on `==` too eagerly → wrong on `[21],h=3` (got 8, answer 7); fixed with `h >= hours` → record + shrink + return `result` [INSIGHT] (**M-013**: never return on an exact match in a find-minimum search). All 6 test cases pass. O(n·log k)/O(1). (Day-10 = single new problem per roadmap.)

## Day 11 — 2026-07-01 — Find Min in Rotated Sorted Array (#153) + variable-audit drill
Block 1 (4 due): first pass had TWO wrong-variable slips — #125 `strs` vs `s`, #167 `nums` vs `numbers` (#875 Koko + #11 Container clean). That's **M-004 recurrence 3 → escalated to BLOCKERS**; introduced the **"variable audit before submit"** drill → fixed both instantly → all 4 clean, and **#125 finally green** after 5 sessions [INSIGHT]. #153: derived the `mid` vs `nums[right]` invariant himself; tripped on `while left <= right` + `right = mid` → **infinite loop** (**M-014**), fixed with strict `<` + `return nums[left]` [INSIGHT]. Verified 7/7. O(log n)/O(1). Also retired the roadmap's stale review column — reviews now come only from `QUEUE.md`.

## Day 12 — 2026-07-02 — Interleave day (#238, #3, #153) + variable-audit drill
Mixed unlabeled set, name-the-pattern-first; no new problem. Warm-up 3/3 reflexes clean (`.append()`, converging `<`, record-and-shrink). **#238 Product** ✅ correct, O(n)/O(1)-extra, prefix/suffix — but named it "iteration of lists" → corrected to **prefix & suffix products**; audit caught `appened`→`append` (M-004/B-1, needed a nudge) [STRUGGLE]. **#3 Longest Substring** ✅ correct, unprompted amortized O(n), audit clean [INSIGHT]. **#153 Find Min Rotated** — correct invariant + both loop reflexes, but regressed from Day 11's `return nums[left]` to tracking `answer = 0` → returned 0 on `[2,1]` and single-element `[5]` (**M-015**); found it by tracing, self-fixed back to `return nums[left]` [STRUGGLE][NEEDS_RECALL] → **reset to 1d**. Keeper: in a converging search the exit index IS the answer — no tracked candidate. Pattern names 2/3 crisp.

## Day 13 — 2026-07-03 — Binary-search reviews + Search in Rotated (#33) + first Stack (#20)
Block 1 (3 due, all **PASS**): #704 ✅ (slip: `for`→`while`, M-016, self-owned); #74 ✅ clean (flattened index); **#153 ✅ retest of the Day-12 fail — `return nums[left]`, no tracked answer; M-015 lesson stuck** [INSIGHT]. Block 2 new: **#33 Search in Rotated** — hardest yet; built which-half-is-sorted + the nested range-test himself [INSIGHT], but inverted ALL FOUR pointer updates (**M-012 recurrence 2**) → fixed on "pointer = opposite wall from direction" [STRUGGLE]; O(log n)/O(1). **#20 Valid Parentheses — first STACK** (pre-taught LIFO list): 3 bugs ironed out — empty-arg `append`, dict looked up the popped opening not the current closing, and a bare `return True` (band-aided with `len(s)<2` before landing on `return len(stack)==0`) [STRUGGLE]; O(n)/O(n). New pattern file: `stack.md`. No wrong-*name* slip today; B-1 audit still active.

## Day 14 — 2026-07-05 — Stack reviews + Min Stack (#155, first class)
Block 1 (3 due, all **PASS**): **#33** ✅ all four directions correct cold — M-012 inversion fixed [INSIGHT]; **#20** ✅ all 3 bugs gone, clean cold; **#125 Valid Palindrome ✅** clean (`clean` var + `.isalnum()` both correct) — a real B-1 win on the blocker problem [INSIGHT]. Pre-taught **Python classes** (class/`__init__`/`self`/methods; instance = the object before the dot). Block 2 new: **Min Stack (#155)** — first class; two-stack technique. Sharp questions (why not a single min var → pop can't recover the previous min w/o an O(n) scan; conditional push + `<=` for dup mins). Slips: `val <= self.stack[-1]` should be `self.minStack[-1]` (**M-004 recurrence** — stack vs minStack) [STRUGGLE]; empty-guard `>0` vs `==0`; class named `Stack` not `MinStack`. Final correct, all ops O(1)/O(n). **B-1: honest hold** — #125 clean but the #155 wrong-container slip keeps the blocker active one more session. stack.md +Min Stack template, mastery 1→2.

## Day 15 — 2026-07-06 — Stack reviews + RPN (#150) + Daily Temperatures (#739, monotonic stack)
Block 1: **#875 Koko ❌** reset 1d — converging search with a tracked `answer=0` returned instead of `left`; leaked 0 when the answer = top of range (**M-015 recurrence 2**) [STRUGGLE]; **#153 ❌** reset 1d — `right = mid - 1` instead of `right = mid`, discarded the candidate min, failed `[3,1,2]` (**M-017**) [STRUGGLE]; **#155 Min Stack ✅** clean (correct `self.minStack`, `MinStack` named). Consolidated the converging checklist (`< → right=mid → return nums[left]`). Block 2 new: **#150 RPN ✅** — stack eval; bug = operand order on `-`/`/` (a/b reversed) → fixed; taught `int()` + `int(a/b)` truncation. **#739 Daily Temperatures ✅ — first MONOTONIC STACK** (indices, `temps[stack[-1]]`); bugs = over-pop (missing temp condition) + empty-guard order → taught **short-circuit evaluation** (guard first) [INSIGHT]; O(n)/O(n). B-1 clean on names today (1 of 2 to clear). **Coaching: Parthiv asked for less spoon-feeding from Day 16 on.**

## Day 16 — 2026-07-07 — Converging retests + 3Sum (#15) + Longest Repeating Char Replacement (#424)
Block 1 (3 reviews, all **PASS** — converging checklist stuck): #875 ✅ (`return left`, `right=mid`), #153 ✅ (`right=mid`, `return nums[left]`), #739 ✅ (guard-first monotonic). Both Day-15 converging fails recovered [INSIGHT]. Block 2 new (first **less-spoon-feeding** session — he derived both): **#15 3Sum ✅** — sort → pin `i` → two-pointer for `-nums[i]` → dedup pin + left; bugs: `==` branch no pointer move (infinite loop), `left=0`→`i+1`, two-level dedup; deep dive on why left-skip alone suffices [INSIGHT]; O(n²). **#424 ✅** — sliding window, window valid while `L-maxFreq<=k`, freq dict; bugs: `len(range(s))`→`range(len(s))` (**M-003 ×3**), stale `L` in shrink loop (infinite loop → `L-=1`), returned last window not max (→ `answer=max`); O(n)/O(1) (dict ≤26). **🎉 B-1 CLEARED** (2 clean-on-names sessions). **M-003 → B-2** (light: range/len order).

## Day 17 — 2026-07-08 — RPN/#15/#424 reviews + Longest Consecutive Sequence (#128) + Encode/Decode Strings (#271)
Review-heavy (8 due) → ran 3 fragile 1d items, rolled 5 stable to Day 18 interleave. **#150 ✅** (space O(1)→O(n), M-005); **#15 3Sum ❌ reset 1d** — omitted the `while left<right` wrapper so the two-pointer never swept (**M-018**), fixed on nudge [STRUGGLE]; **#424 ✅** clean, correctly called O(1) space (dict bounded at 26) [INSIGHT]. Block 2 new (less-spoon-feeding, both derived): **#128 ✅** — set + only-start gate (`x-1 not in set`) + forward count; worked out amortized O(n) himself [INSIGHT]; bug: inverted `not in`→`in`. **#271 ✅** — length-prefix (`len#str`), decode by COUNT not delimiter; class w/o `__init__`; bugs: encode omitted/overwrote word→`+=`, decode `i`-reset/multi-digit/`j`-reset. **Space-complexity theme (M-005 ×2):** said O(1) for O(n) three times, corrected each — but improving (nailed #424 + #271-encode). **B-2 clean today** (1 of 2).

## Day 18 — 2026-07-09 — Interleave day (6 unlabeled reviews)
Mixed unlabeled set, reviews-only (backlog). **#15 3Sum ✅** reset cleared (`while` wrapper present, both dedups, space O(n) unprompted); **#128 ✅** set + only-start, `in seen`; **#20 ❌ reset** — dropped the empty-stack guard → crashes on `")("` (**M-011 ×2**), fixed on nudge [STRUGGLE]; **#3 ✅** sliding window (7d→21d); **#271 ✅** length-prefix, decode-by-count — slip `while n in range(l)`→`for` (**M-016 ×2**); **#33 ✅** rotated search via `nums[mid]>nums[right]` sorted-half check. **🎉 B-2 CLEARED** (2 clean range/len sessions — both blockers now clear). **Space reflex held all day** — zero O(1)-for-O(n) slips [INSIGHT]. Named all 6 patterns cold.

## Day 19 — 2026-07-10 — Linked Lists: Reverse (#206) + Merge (#21) + Cycle (#141) + 5 reviews
New review protocol's first run (budget/verbal/fuzz). Block 1 (5): #20 ✅ reset cleared → 3d; #155 ✅ (dropped `self.` on minStack ×2, nudged) → 7d; **#875 ❌ reset** — `left=0` → `ceil(1/0)` div-by-zero on `[1]` (**M-019**); #153 ✅ → 7d; #121 ✅ → 21d (verbal tier). **Linked lists (new):** pre-taught ListNode + built an interactive reversal-stepper widget; Parthiv flagged under-drilling → ran a full **basics drill** (build/traverse/access/delete/insert) — the day's turning point [INSIGHT]; core lesson = **node vs value**. **#206 Reverse ✅** (prev/curr/nxt; **forgot `return prev` → M-001 ×3 → blocker B-3**); **#21 Merge ✅** (dummy+tail; a node ref IS the whole rest of the chain [INSIGHT]); **#141 Cycle ✅** — Floyd's, reasoned "two runners must meet on a loop" himself; bug: `None==None` false positive → `and fast != None` guard. All O(n)/O(1). New `patterns/linked-list.md`. **System lesson: drill a new data-structure's basics before manipulation problems.**

## Day 20 — 2026-07-11 — Linked List mediums: Reorder (#143) + Remove Nth (#19) + 7 reviews
First full run of the new protocol (verbal tier + logged times). Block 1 full re-solves: #875 ✅ reset cleared (`left=1`, 5:54); #150 ✅ (8:03); **#739 ❌ reset** — dropped the empty-guard + forgot `stack.append(i)` (**M-011**); #167 ✅ → 21d verbal (6:30); #11 ✅ → 21d verbal (5:43). Verbal: **#49 ✅** → 60d; **#242 ❌** — recalled WRONG pattern (two-pointer instead of freq-count) → full solve → dropped the length guard (**M-011, literal Day-7 repeat**) → reset. Block 2: **#143 Reorder ✅** — find-middle (fast/slow) + cut + reverse 2nd half + merge; strong first attempt, only a `revCurr`/`reCurr` typo [INSIGHT]; **#19 Remove Nth ✅** — dummy + two-pointer gap of n, clean first try. All O(n)/O(1). **M-011 (dropped guards) ×2 today → escalated to blocker B-4.** B-3 (return) clean today. `patterns/linked-list.md` +find-middle/reorder.

## Day 21 — 2026-07-13 — RECURSION from scratch + first Trees (#226, #104)
**Block 1 (6 due):** #739 ✅ 6:55 (reset cleared — the `while stack and ...` guard was back), #206 ✅ 2:03, #21 ✅ 5:57 (typo, self-caught). **#15 ❌ reset** 12:21 — dedup used `if` instead of `while`, so only ONE duplicate got skipped, not a run [STRUGGLE]. #128 ✅ 8:03 (said O(n) not O(n²), with the reason). **#271 ❌ reset** 11:45 — compared the **index** to `"#"` instead of `s[j]`, forgot `i = j + 1`, read length/content in the wrong order [STRUGGLE]. Rolled: #125, #704.

**The day's real work — recursion.** He asked for it directly: *"I think it is best you drill me on recursion first cause it looks really confusing."* Never used it before. Taught the two-part shape (base case + recursive case), then the **call stack** with an interactive stepper. Drills: traced `fact(4)` by hand ✅ → wrote `power(base, exp)` cold ✅ → `sumOfValues(node)`, where he wrote `return 2 + sumOfValues(left) + sumOfValues(right)`, thinking the constant was the **child count** [STRUGGLE]. **Reframed: the constant is what THIS node contributes** — `node.val` for a sum, `1` for a count/depth. That single sentence is what made #104 land ten minutes later [INSIGHT].

**Block 2 (new):** **#104 Max Depth** ✅ off the template — and he said "O(1) space", then **corrected himself unprompted**: *"if it is stacked up like that it is taking up space therefore, O(n)"* [INSIGHT] — the call stack is real memory, O(h)/worst O(n). **#226 Invert** — attempt 1 swapped `.val` (only flips one level, AND `node.left.val` crashes on every leaf); attempt 2 swapped the **pointers** correctly. Missing `self.` on the recursive calls (M-020, 2nd time).

**Pattern of the day (beyond recursion): container vs. contents** [INSIGHT] — the #271 index-vs-char bug and the #226 node-vs-`.val` bug are the *same mistake* an hour apart. Logged as **M-021**.

**Blockers:** **B-3 (forgetting `return`) CLEARED** ✅ — 8-for-8 today, two clean sessions running. **B-4 (edge-guards) still active** — #226's leaf crash resets its count to 0 of 2.

**Post-session (he asked, unprompted):** *"Why are we not doing `self.node.left`? How do I know when to include `self.`?"* — taught that **`self` is just a variable naming the object the method was called on**, so the only question is *whose thing is it*: `invert` is the Solution's → `self.invert()`; `left` is the TreeNode's → `node.left`; `self.node` is nonsense because a *parameter* lives on no object [INSIGHT]. Gave him the test (*attached in `__init__` or a method of this class? → `self.`; parameter or local → bare*); he applied it correctly to #104 **and correctly left `root` and `leftDepth` bare without being told** — so it's the rule, not pattern-matching. Wrote it up as **`patterns/python-classes.md`** — it's load-bearing for #543 tomorrow (**local = one per call · `self.` = one per object**; the running max must be `self.diameter`), and for every design problem after.

**Schedule:** the recursion hour displaced **LRU Cache (#146)** → **rescheduled to Day 24 (Jul 16)**, the only day with Block-2 slack. Aug 20 target unaffected. New pattern file: `patterns/trees.md`. **Patterns learned: 8.**

## Day 22 — 2026-07-14 — Trees: Diameter (#543) + Same Tree (#100) + LCA BST (#235) + 8 reviews
**Block 1 (8 due, 6 of 8):** #15 ✅ 7:35 — **reset cleared**, the dedup `while` held. #226 ✅ 2:32 and #104 ✅ 3:23 (carried **O(h)** over unprompted one problem after learning it [INSIGHT]). #125 ✅ 4:45 → 21d, and he'd guarded the no-alnum edge (`" ."`) without being told. Verbal #1 ✅ / #217 ✅ → 60d — *but he stated both loop bodies in the wrong order* ("add, then check"), which as described returns `True` on `[1,2,3,4]`; had the right model, described it imprecisely [STRUGGLE]. **In an interview the interviewer only has what you say.**
**#271 ❌ reset** 8:37 — `while j != "#"` compares the **index** to a **char** (needs `s[j]`). **This is the literal reason #271 was reset on Day 21.** Same slip, same problem, second time → **M-021 recurrence 4 → escalated to blocker B-5** [STRUGGLE][NEEDS_RECALL].
**#242 ❌ reset** 3:11 — dropped the `len(s) != len(t)` guard; `"aab"`/`"ab"` returns True. Needed the counterexample handed to him. ***"Lengths equal?" is literally on his own pre-submit checklist*** (**M-011 / B-4**) [STRUGGLE]. Upside: probed on the constraint (lowercase letters) he corrected space O(n) → **O(1)** — the map is bounded at 26.

**Deep dive that ate the review block — `sorted()` vs `.sort()`** [INSIGHT]. He said #15's O(n) space came from `alist = sorted(nums)` ✅, then said `nums.sort()` "keeps it the same" ✗. Taught: **mutating methods return `None`; builder functions return the object.** So `alist = nums.sort()` sets `alist = None` → `TypeError` — it doesn't optimize, it crashes. `.sort()` → O(1) from your code; `sorted()` → O(n). Also: **"in-place" ≠ free** (Timsort needs O(n) scratch worst case), and **output doesn't count** toward space.

**Block 2 — the box outside the recursion (new mental model).** Taught in isolation before #543, as he asked. **`dfs` returns what the PARENT needs; `res` collects what YOU need.** The proof that the box is *mandatory*: on a tree whose diameter bends at a non-root node, the root's `left+right` gives the wrong answer — the return pipe carries one number (a height) and **has no memory of the path running across a child** [INSIGHT]. Taught the `UnboundLocalError` trap (`self.res` / `nonlocal`) and the 6-slot anatomy (only slots 5 and 6 ever change; **`dfs(root)`'s return value is discarded — you call it for the side effect**).
Also had to teach **what "DFS" means** — he'd never been given the word, though he'd been writing it since Day 21.
**Warm-up drill (count nodes via the box):** wrote `self.res += left + right`, then `self.res += 1 + left + right` — both wrong. `left`/`right` hold **heights**, not counts; **he kept bolting them on because they were sitting there in scope** [STRUGGLE]. → ***Being in scope doesn't mean being relevant.*** M-021, third costume of the day. Answer: `self.res += 1`.

**#543 Diameter ✅** — correct first try once the model landed, complexity cold. Spotted the real point himself: **when a child's result feeds BOTH the box and the return, you must capture it in a variable** — inline it and you call `dfs` twice, re-walking the subtree [INSIGHT]. Threaded `dfs(node.left)` (nested fn, no `self`) vs `self.res` (attribute) unprompted.
**#100 Same Tree ✅** — first instinct was `p.left.val` (= the Day-21 `None.val` crash); reframed to ***stop peeking down; the base case is where `None` is handled***. Found the crux himself: neither base case works alone — `and` (both None → True) must come **before** `or` (exactly one → False). Then asked *"doesn't it just return True after the value gate?"* → counterexample (left-leaning vs right-leaning `1/2`) → **`isSameTree(p,q)` means the ENTIRE SUBTREE matches, not the two nodes** [INSIGHT]. **Correctly judged that no box was needed** — the return channel wasn't spoken for.
**BST taught cold** (he'd never met one): the invariant is **subtree-wide, not parent-child**; it turns a tree into binary search (**eliminate without visiting** → O(h)); hitting `None` means *definitively absent*. Got the validity trap right (`4` under `7` under `5`). **The move: ordinary tree → explore BOTH children; BST → compare and pick ONE.**
**#235 LCA ✅** — derived all three cases; first instinct on the split case was "backtrack to find where they meet" → showed that stepping either way **abandons one of `p`/`q`**, so **the split IS the answer, immediately** [INSIGHT]. Equality falls into the `else` for free — *the strictness of `<`/`>` is what lets it*. Two bugs: **inverted the descent direction** (**M-012 recurrence 3 → escalated to blocker B-6**) and **no loop at all** — reassigned `node` then fell off the bottom (**M-018 recurrence 2**). Final: `while` + O(h) time, **O(1) space — no recursion, no call stack**, which he flagged himself ✅.

**The day's theme, and it is not trees:** he **derived every algorithm** — the box pattern, the base-case split, the LCA rule — and then **lost to his own first draft**: index-vs-char (#271), heights-as-counts (the res-drill), missing length guard (#242), `if`-where-a-`while`-belongs (#235's descent, M-018 ×2), inverted direction (#235). **He knows the algorithms. He is losing to precision.** New reflex list: **box or contents? · is it guarded? · am I looping where I should loop?** *(Credit where due: **#15's dedup `while` held** — M-022 dormant.)*
**Blockers:** **B-4 still active** (#242 — count resets to 0 of 2). **B-5 NEW** (M-021, container-vs-contents). **B-6 NEW** (M-012, search direction). New pattern file: `patterns/binary-search-tree.md`. **Patterns learned: 9.**
**New standing instruction (COACHING #8):** *"Give me the examples with the questions too"* — every problem gets **one short** input/output example, not the full LeetCode dump.

## Day 23 — 2026-07-15 — BFS from scratch (#102, #199) + Balanced Tree (#110) + 7 reviews
**Block 1 (7 due, 5 of 7).** **Both blocker problems came back CLEAN** — **#271 ✅ 9:19** (reset ×2 cleared — `while s[j] != "#"`, the exact bug that reset it twice, is gone; went deliberately slow) → **B-5's first clean rep**; **#242 ✅ 4:05** (length guard present, wrote himself a reminder in the comment, O(1) space unprompted) → **B-4's first clean rep**. #543 ✅ 5:01, #100 ✅ 5:45, #141 ✅ 4:46 (guarded the single-node `None==None` trap — B-4 behavior) — all clean cold.
**#235 ❌ reset** 3:26 — **inverted BST direction AGAIN** (both-smaller → wrote `node.right`; his own comment said "go left"). **B-6, 4th occurrence, same problem two days running.** [STRUGGLE] **Root-cause fix installed: write the comparison TARGET-first** (`p.val > node.val → right`) so the direction word matches and there's nothing to mentally flip — the flip was where he kept inverting.
**#143 ❌ reset** 12:41 — phases 1&2 (find-middle + reverse) correct; **phase 3 imported #21 Merge's VALUE comparison** (`if curr1.val <= curr2.val`) into a problem that **forbids touching values** — reorder strictly alternates. Also `curr2 = rev` NameError. [STRUGGLE] **His own diagnosis was spot-on:** *"I dumped what I practiced for merging and instinctively used the sort value checker."* → **M-009 recurrence 2** (carrying assumptions across problems). **Lesson: pattern recognition gives the SKELETON, not the BODY — re-derive what decides each step. The tell was in the spec ("may not modify values") — a value comparison there is a red flag.**

**Block 2 — BFS taught from scratch** (he flagged it as hard to visualise, so taught in isolation with the coffee-shop-line model for the deque, a full step-by-step queue trace, and both-versions trace of why `level_size` must be frozen). Core ideas: **FIFO not LIFO** (queue, not call stack); **popleft is O(1), list.pop(0) is O(n)**; **the queue at the top of each round IS exactly one level** → freeze `len(q)` before appending the next level's kids. **#102 Level Order ✅** first try. **#199 Right Side View ✅** (`level[-1]`). **#110 Balanced ✅** — reasoned out himself that the boolean needs a **box** while the return carries the height (**#543's box pattern transferred to a new problem**); wrote the **"only-ever-falsify"** update `abs(left-right) <= 1 and self.res` — the analog of #543's `max` — without being told [INSIGHT].

**The day's theme — the fix is working where it's practiced.** Yesterday he lost to imported templates/complexities. **Today in Block 2 he re-derived all three complexities from scratch instead of recalling** — first wrote #102 as "O(h)/O(1)" (DFS reflex), then corrected to **O(n)/O(n)**, and nailed the **DFS-space-is-height / BFS-space-is-width** contrast. **Three different space complexities stated correctly in one block: DFS O(h), BFS O(n), iterative-BST O(1)** [INSIGHT]. But Block 1 shows the same imported-template failure mode is still live in review (#235 direction, #143 merge-body) — the two are the same root disease, and both now have a concrete drill.
**Blockers:** **B-4 and B-5 each got their FIRST clean rep** (1 of 2 — one more session clears both). **B-6 fired again** (0 of 2). **M-009 → recurrence 2** (watchlist). New pattern: BFS added to `patterns/trees.md`. **Patterns learned: 9** (BFS is part of Trees). **#98 Validate BST deferred Day 23 → Day 25** (did #199+#110 instead; #98 clusters with #230). Queue rebalanced — Jul 16–18 were clustering, stable 7d items fuzzed forward.

## Day 24 — 2026-07-16 — LRU Cache (#146) — LAST CORE PROBLEM ✅ + interleave (6/6)
**Block 1 (interleave, 6 due, 6 of 6 ✅) — and both Day-23 resets came back fixed.** **#235 ✅ 6:25** — wrote it **target-first** (`p.val > node.val → right`), no mental flip → **B-6's first clean rep on the exact problem that beat him twice.** **#143 ✅ 14:20** — phase-3 merge is now **strict alternation, no value comparison** (he wrote "there is no comparison needs to be done" in his own plan) → **M-009 lesson landed.** #19 ✅ 7:26 (dummy handles remove-the-head), #739 ✅ 8:44 (empty-stack guard present in both if and while — B-4 behavior), #206 ✅ 3:19 (return prev), **#21 ✅ 5:15 — the interleave payoff: took from the smaller VALUE here, 20 min after strictly ALTERNATING on #143, picking the right merge body for each WITHOUT the pattern being named** [INSIGHT]. **All three open blockers (B-4/B-5/B-6) now riding clean → could all drop after Day 25.**

**Block 2 — LRU Cache (#146), the LAST UNPLACED CORE PROBLEM ✅.** Taught in layers first (design problem, all-new machinery): what LRU is → why dict-alone (O(n) eviction) and list-alone (O(n) lookup) each fail → **use both** → the **doubly-linked list** (backward pointer makes O(1) removal of a held node; he read the pointer surgery back correctly) → **dummy head/tail sentinels** (every real node guaranteed a prev/next → the `None` special-cases vanish, i.e. his B-4 guard problem designed away) → **dict maps key→Node**, and **that's why Node stores its own key** (needed for `del adict[node.key]` at eviction).
**His build: strong structure, four precision bugs — his exact profile.** `insert_front` (4-pointer stitch) and `remove` structurally correct first try. Bugs: (1) **`self.node` on a parameter** and (2) **`remove(node)` called without `self.`** — both **M-020, the `self.` rule from Day 21**, self-corrected with the test; (3) **`get` fell off the end with no `return node.val`** — **M-001/B-3 recurrence, first since it cleared Day 21**; (4) **dual-structure sync (M-024 NEW)** — updated the list but forgot the dict, twice (overwrite didn't repoint `adict[key]`; eviction didn't `del adict[lru.key]`), plus an off-by-one on the capacity check (check-before-add). **Final verified in sandbox: spec example + overwrite + capacity-1 + missing-key + 2000 randomized trials vs OrderedDict — all PASS.** Complexity O(1)/O(1), space **O(capacity)** (two different "n"s — be explicit).

**Milestone:** every core roadmap problem is now covered before Aug 20. What remains is depth (Tries/Heap/Backtracking/Intervals/Graphs/DP), not core gaps.
**The theme, again:** he built the hardest data structure in the sprint correctly — the bugs were all first-draft precision on machinery he understood (`self.`, return, dual-sync). **Reasoning strong, first draft imprecise** — unchanged profile.
**Blockers:** **B-7 escalated — the `self.` rule (M-020 → recurrence 3)**; he knows the rule but doesn't run it on first draft (same shape as B-4). **B-3 (return) recurrence → reopened as a watch** (one slip in the hardest problem; standing rule says re-escalate on recurrence — pre-submit "does it return?" mandatory again). **M-024 NEW — dual-structure sync.** B-4/B-5/B-6 all clean today (each 2 of 2 pending confirmation next session... actually 1-of-2 → but clean today; see BLOCKERS). New machinery in `patterns/linked-list.md` (doubly-linked list + hashmap combo). **Patterns learned: 9** (LRU is a design problem using existing patterns + new machinery, not a new algorithmic pattern).

## Day 25 — 2026-07-17 — Validate BST (#98) + Kth Smallest (#230) + FIRST TRIE (#208) + 5 reviews
**Block 1 (5 due, 2 of 5 — rough, but all surface, not comprehension).** **#146 LRU ❌ reset** 35:16 — full blank-screen rebuild; **all three drilled behaviors held on the first draft** (`self.` perfect → B-7's 1st clean rep · `return` present → B-3 held · **dict synced in both paths → M-024 stuck**), but three bugs needed hints: **incomplete `addFront` (2 of 4 pointers)**, `Node()` missing args, `del adict(...)` parens. **#102 Level Order ❌ reset** 10:52 — algorithm perfect, two API-recall crashes (`from * import` → `from collections import deque`; `popfront()` → `popleft()`). **#199 Right Side View ✅** 4:24 → 3d — correct AND reconstructed the optimized single-value variant from a glimpse [INSIGHT]. **#226 Invert ❌ reset** 5:52 — **swapped LOCAL `left`/`right` instead of `node.left`/`node.right`** → tree untouched; container-vs-contents family (B-5) in a new costume [STRUGGLE]. **#104 Max Depth ✅** 2:34 → 7d, clean.
**The real signal:** #146 (incomplete `addFront`) and #226 (swapped locals) are the **same shape — pointer/field assignments that don't do what you think** → new mistake **M-025**, drill: ***"am I mutating the object's field or a local? · count the pointers"*** (insert-between = 4 pointers, remove = 2).

**Block 2 — 3 new, ALL ✅.** **#98 Validate BST ✅** — taught the **`(low, high)` bounds passed DOWN**; going left tightens `high`, going right raises `low`, the bound propagates so `4` under `8` under `5` is caught. He filled all six bounds correctly. **New tool: passing state DOWN via parameters** (vs #104/#543 passing answers UP via return) — both DFS [INSIGHT]. **#230 Kth Smallest ✅** — **in-order = sorted**; big call-stack teaching moment (*"5 is already queued right, how does 4 come first?"* → traced frame-by-frame: 5 is PAUSED at step 1, can't visit itself until its whole left subtree drains; **the node visits itself BETWEEN its two recursive calls**) [INSIGHT]. List version correct; said space O(h) then **self-corrected to O(n)** (the list holds all n — M-005). Taught the early-stop optimization + the **flag-every-frame-checks** mechanic for stopping a recursion. **#208 Implement Trie ✅ — FIRST TRIE, clean first try, zero bugs.** Pre-taught the structure in isolation; the key unlock came from his own question (*"don't I need a `self.val`?"* → **no: the character is the KEY in the parent's `children` dict, not a field on the node**). His synthesis: *"a Trie is nodes attached to dictionaries, like LRU but each node has its own children dict"* — exactly right. insert/search/startsWith clean; verified vs a reference set on 1000 trials.

**The theme:** **Block 2 strong (3/3 new, incl. a new data structure clean cold); Block 1 rough (3 resets) but every failure was surface** — API names (#102), incomplete/mis-targeted pointer writes (#146, #226) — **not comprehension.** The drilled precision items (`self.`, return, dict-sync) all held.
**Blockers:** **B-7 (self.) 1st clean rep ✅ 1 of 2.** **B-3 (return) clean, watch holding.** **B-5 (container-vs-contents) watch reopened** — #226 local-vs-field is the same family. **M-025 NEW** (pointer surgery). **M-005 recurrence** (self-corrected w/ nudge; kept on watchlist, not escalated — space is otherwise a 4/5 strength). **B-6 unchanged** (no direction-choice problem today — awaits #235 on Jul 19). New pattern file `patterns/tries.md`. **Patterns learned: 10** (Tries added).

## Day 26 — 2026-07-18 — Trie wildcard (#211) + FIRST HEAPS (#703, #1046) + 6 reviews
**Block 1 (6 due, 4 of 6) — both Day-25 pointer-surgery resets came back FIXED.** **#146 ✅ 16:10** (reset cleared — all 4 pointers in addFront, self. clean, dict synced; verified vs OrderedDict; down from 35→16 min), **#226 ✅ 5:54** (reset cleared — swapped node.left/right FIELDS not locals) → **M-025 fix held on both.** #98 ✅ 4:16, #230 ✅ 4:34 (wrote the **optimized early-stop version unprompted**, better than Day-25's list version). **#102 ❌ fail** 7:38 — API names fixed but **dropped BOTH None-guards he had on Day 23** (root-None + child-None before enqueue → None.val crash) → **B-4 recurred** (cleared Day 24). **#208 ❌ fail** 7:13 — **forgot `node.isEnd = True`** at end of insert → every search False.
> **Coach error owned:** stated #230 example as "k=3 → 5"; correct is **4** (in-order 1,3,4). His code right, my annotation wrong (also wrong Day 25). Fixed.

**Block 2 — 3 new, all solved w/ guidance.** **#211 Add & Search Words (Trie + `.` wildcard)** — he got the conceptual leap himself (`.` → DFS into ALL children, backtrack), but execution had **5 precision bugs** (addWord missing isEnd AGAIN, base-case off-by-one, 2 missing returns, wildcard giving up after first child). **Built two step-through animations** (trie+code-line sync, and call-stack push/pop/backtrack on `.at` over {bad,cat}) — the stack viz is what made wildcard backtracking click [INSIGHT]. **He asked to always go slow on recursion-heavy topics → COACHING rule #9 added.** **Heaps pre-taught in isolation** (first heap, NOT recursion — a breather): `heapq` min-heap, push O(log n)/pop O(log n)/peek O(1), negate for max-heap. **#703 Kth Largest ✅** — the **min-heap-of-size-k** design (h[0] = the cutoff = kth largest); he was stuck on the insight, taught slowly with concrete numbers; add O(log k). **#1046 Last Stone Weight ✅** — max-heap via negation, clean. All 3 verified vs brute force.

**Theme — the precision tax MOVED facet:** ✅ M-025 pointer-surgery FIXED (both resets recovered), ✅ **B-7 self. CLEARS** (2 clean sessions). ❌ New facet: **dropping a required line** — None-guards (#102 → B-4 recurred), isEnd end-marker (#208, #211), returns (#211); plus heapq arg-count friction ×3 (new-API). **Same root — first-draft completeness.** The scan: *walk the op to the end — guard? end-marker? does every branch return? all args passed?*
**Blockers:** **B-7 (self.) CLEARED ✅.** **B-4 (guards) RECURRED → reopened watch.** **B-3 (return) recurred (#211) → watch holds.** **M-026 NEW** (dropped the terminal `isEnd` line, #208+#211). M-025/B-5 clean today. **B-6 pending #235 (now due Jul 20).** New pattern file `patterns/heap.md`. **Patterns learned: 11.**
*(End of Sprint Week 3. Jul 19 = rest day. Next: Day 27, Jul 20.)*

## Day 27 — 2026-07-21 — Heap depth (#973 K Closest, #215 Kth Largest) + 7 reviews  *(Jul 20 skipped; ran Jul 21)*
**Block 1 (7 fragile worked, 4/7; 6 stable 3d rolled forward — overflow).** **#102 ✅ 5:48** (reset ×2 cleared — both None-guards present first draft; space O(n)=widest level, unprompted) → 3d. **#208 ✅ 7:50** (reset ×2 cleared — `isEnd` present first draft → **M-026 first clean rep**). **#211 ❌ reset** — wildcard-DFS **cold-correct** (up from 5 bugs Day 26) but **two ownership NameErrors**: `isEnd`→`node.isEnd`, `root`→`self.root` → **B-7 watch REOPENED** (M-020 ×4) [STRUGGLE]. **#703 ✅ 8:44** (every ref owned; heapq args correct; **complexity drilled O(log k) not O(log n)**) → 3d. **#1046 ❌ reset** — **said "negate" then wrote a plain min-heap**; 3 corrections (no negation → `heapop` typo → `return h[0]` not `-h[0]`). Negation = a **touch-every-boundary transform** [STRUGGLE]. **#235 ✅ 2:27 → 7d — B-6 CLEARED** (target-first, no inversion, `while` loop; rep 2 after Day 24). **Last drill-now blocker — all of B-1…B-7 now clear** [INSIGHT]. **#110 ❌ reset** — code **clean** but stated the box channel **backwards** ("return is always a boolean") until a Socratic walk to "return carries the height, box holds the boolean"; recovered instantly (recursion recall wobble, rule 9; M-028) [NEEDS_RECALL].
**Block 2 — Heap depth, 2 new, both w/ guidance.** Pre-taught **heapq tuple keys**. **#973 K Closest ✅** — proposed min-heap-of-all, traced that popping a min-heap to size k keeps the **farthest** k → flipped to **max-heap-of-size-k via negation**. Bugs: **`^` is XOR not power** (`3^2==1`) [INSIGHT], int-vs-tuple compare, **missing `heappop`** (heap grew past k). Negated compare stayed right — **B-6 transferred** [INSIGHT]. O(n log k)/O(k). **#215 Kth Largest ✅** — recognized the **#703 engine**; bugs: `maxHeap < k` (missing `len()`), **rename straggler**. O(n log k)/O(k). *(Quickselect deferred.)*
**Theme — the disease moved to the EDIT level: M-027 (NEW): a multi-site change (negate/rename/own/evict) where the first draft missed a site.** Algorithm right in his head every time. **Blockers:** **B-6 CLEARED ✅** (→ dormant). **B-7 WATCH REOPENED** (#211). **M-027 NEW** (×3: #1046/#215/#973). **M-028 NEW** (box channel). **Patterns: 11.** Pace: Jul 20 skipped; 30 days to Aug 20; core done — absorbable.

## Day 28 — 2026-07-22 — Backtracking from scratch (#78 Subsets, #39 Combination Sum) + 6 reviews  *(2nd session of the day — makes up the skipped Jul 20)*
**Spacing call:** did NOT re-drill the 5 items done minutes ago on Day 27 (#211/#1046/#110/#973/#215 — no spacing value); pulled genuinely-due items instead.
**Block 1 (6 real spaced reps, 3/6).** **#271 ✅ 8:38 → 7d** — decode compared `s[j]` not the index (the container-vs-contents bug that reset it twice is GONE); round-trips w/ `#` inside a word + multi-digit lengths. **#143 ❌ reset** — 3-phase (find-mid/reverse/alternate-merge) **flawless**, incl. "no value comparison" (M-009 lesson held), but **crashed on `curr` for `curr3`** (NameError not self-caught) [STRUGGLE]. **#226 ✅ 3:47 → 7d** — swapped the FIELDS via temp (M-025 held). **#98 ✅ 11:59 → 7d** — bounds passed down, strict chained compare (held from Day 25). **#199 ❌ reset** — BFS clean but **dropped the root-None guard** (B-4) AND **called space O(1)** (M-005, it's O(n)) — *both of which he got right on #102 the day before* → real decay surfaced by a genuine spaced rep [NEEDS_RECALL]. **#146 ❌ reset** — cold rebuild 15:38, drilled parts HELD (dict-sync M-024, 4-pointer stitch M-025, `self.` at 5 sites), but **dropped `self.` on one call** (`remove(lru)`) → **B-7 recurrence, 2nd day running** [STRUGGLE].
**Theme — every reset was ONE thing: reconstruct a correct algorithm/design cold, lose exactly ONE site on the final pass** (curr typo · dropped guard · missing self.). **B-7 (self./ownership) fired Day 27 AND Day 28 → RE-ESCALATED to active drill-now.** The single habit that converts today's 3 resets to passes: a final read-through checking every site (guard present, every name real, every method owned).
**Block 2 — BACKTRACKING taught from scratch (new pattern).** He was "lost from the start" → restarted fully concrete (rule 9): listed subsets of [1,2] by hand → reframed as take/skip **decisions** → decision tree → the choose/explore/**un-choose** skeleton, hand-traced frame-by-frame. **#78 Subsets ✅** — **derived by hand-tracing**, code fell out; 2 bugs (`stack()`→`[]`, skip `backtrack(i)`→`i+1`). O(n·2^n)/O(n) — taught why n·2^n (copy each of 2^n leaves costs O(n)). **#39 Combination Sum** — his attempt tangled state (used `self.total` with `+=` but no undo; take used `i+1`, losing reuse). Taught the clean template: **total as a PARAMETER auto-unwinds** (no `-=`), **take STAYS on i** (reuse), success/overshoot base cases split. **Built an interactive call-stack animator** (`backtracking-stack-animator` artifact) — stepping through push/pop of frames is what made **recursion unwinding** click (return cascades through S-frames, halts at first T-frame) [INSIGHT]. Complexity contrast taught: subsets O(n·2^n)/O(n) vs combo-sum O(N^(T/M+1))/O(T/M) — **reuse decouples depth from n**.
**Blockers:** **B-7 → RE-ESCALATED to drill-now** (2 days running). M-027 (multi-site) still watch. M-028 (box), B-4 (guards), M-005 (BFS space) watches. New pattern file `backtracking.md`. **Patterns learned: 12.** Pace: two sessions today; next **Day 29 — Backtracking cont. (#90 Subsets II, #40 Combination Sum II) or Permutations (#46)**.

## Day 29 — 2026-07-23 — Backtracking cont. (#90 Subsets II, #46 Permutations) + 8 reviews + B-7 drill
**B-7 micro-drill (start):** ownership fragments — revealed the real gap is the **nested-function (bare) vs class-method (`self.`)** distinction; his understanding is sound (he'd correctly flagged the question was ambiguous). B-7 is an *execution* slip, not knowledge — so the drill is the final read-through, not the rule.
**Block 1 (8 fragile, 5/8).** **#211 ❌ reset** — ownership HELD (self.root/node.isEnd/bare dfs all correct — the drill worked on that facet!), but 3 *other* precision slips: `TrieNode` missing `()`, `for child in node.children` (keys not `.values()`), dropped `return False` [STRUGGLE]. 21 min. **#1046 ❌ reset** — `heappop(-maxHeap)` (negated the list) + **`return maxHeap[0]` not `-maxHeap[0]` — the SAME return-negation that reset it Day 27** [STRUGGLE]. **#110 ✅ 3:06 → 3d** (box channel correct — the recall wobble was my fuzzy "box" terminology, his model was right: helper returns height, self.res is boolean; nitpick: method named `isValidBST` from copy-paste). **#973 ✅ 6:42 → 3d** (`x*x`, size-k, direction all clean — first cold re-solve). **#215 ✅ 3:20 → 3d** (both Day-27 bugs gone). **#143 ✅ 6:32 → 3d** (`curr3` typo GONE — the exact Day-28 reset cause fixed). **#199 ❌ reset** — guard + O(n) space fixed (Day-28 causes cleared!), but **flipped the enqueue order without flipping the "keep"** → returned the LEFT view; needed a trace nudge [STRUGGLE]. **#146 ✅ → 3d** — cold rebuild, everything clean (dict-sync, 4 pointers, self.), one `self.` missing on `remove(lru)` which he says was a **paste drop** (the sibling branch had `self.remove` correct, corroborating) → counted PASS. Space **O(capacity)**.
**Block 1 theme — the named checks are HOLDING** (self. #211/#146, curr3 #143, guard+space #199, box #110 — every prior reset-cause was correct first-draft). The 3 resets were the hardest problem (#211, 3 fresh facets) + two fresh slips (#1046 return-negation AGAIN, #199 enqueue direction). **The costume closet is emptying.** **B-7 held in his actual writing today (1 clean session — clear after 1 more).**
**Block 2 — Backtracking cont., 2 new, both ✅.** **#90 Subsets II** — extended #78 with `nums.sort()` + **skip-all-copies** in the skip branch; derived *why* duplicates arise (interchangeable copies) and *why* multi-copy subsets survive (take branch untouched). Built **2 animators** (skip-copies step-through + a **decision-tree-that-draws-itself**) — the tree viz is what made the two-level skip jump concrete [INSIGHT]. O(n·2ⁿ)/O(n). **#46 Permutations** — **DERIVED the whole structure himself** after I (wrongly) dumped the skeleton and he called it out: reframed take/skip-index → **for-loop over ALL nums + `seen` set**, no index, choose/recurse/un-choose with BOTH undos. First permutation, built cold from reasoning. O(n·n!)/O(n).
**Coaching self-correction [FEEDBACK]:** I dumped code on #46; he pushed back ("are you forgetting what you have to do?"). Restarted concept-first, Socratic, concrete — and *that* worked (he derived it). Reinforces COACHING #1/#9: teach the model first, never lead with the skeleton.
**Roadmap note:** #90 is actually a **Day-52** item I slotted early by mistake (banked, not wasted); today's roadmap item #46 done; **#79 Word Search carries to Day 30** (grid DFS + backtracking — the bridge to graphs).
**Blockers:** **B-7 — 1 clean session (drill-now until 1 more).** **M-027 (one-site final pass)** still the through-line — #1046 return-negation is now a **twice-identical** miss (Day 27 + Day 29): *un-negate on the way out.* Patterns learned: 12. **Next: Day 30 — interleave review + #79 Word Search.**

## Day 30 — 2026-07-25 — Interleave review + #79 Word Search (grid DFS)  *(planned Jul 24; ran Jul 25)*
**B-7 micro-drill (start):** ran the ownership check-question again; he answered cleanly (*"you have to call `self.expand` but `dfs` is good as it is"*) — bare-nested-def vs `self.`-method distinction solid. Combined with clean ownership in the reviews → **B-7's 2nd clean session.**
**Block 1 — interleave (mixed, unlabeled, 3/6). The reset-causes MIGRATED — every *older* named facet held; all 3 resets landed on the newest pattern.** **#1046 ✅ → 3d — the un-negate finally landed:** `return -maxHeap[0]` correct first-draft, closing **the sprint's one twice-identical repeat** (missed Day 27 AND Day 29). [INSIGHT] **#211 ✅ → 3d** — the hardest problem; all three Day-29 facets gone (`TrieNode()` parens, `.children` keys, `return False` after wildcard loop) AND ownership held (self.root/node.isEnd/bare dfs) → **B-7 facet clean on the exact problem that fired it.** **#199 ✅ → 3d** — enqueue order + "keep" now consistent (Day-29 cause fixed); guard + O(n) space still held. **#78 ❌ · #90 ❌ · #46 ❌ — all 3 backtracking reviews reset → 1d.** Honest read: backtracking is <1 week old (taught Day 28; #90/#46 only Day 29) and the resets clustered *entirely* here while every older facet held — expected fragility on the freshest material, exactly where the ladder should spend resets now.
**Block 2 — #79 Word Search (grid DFS + in-place backtracking), NEW.** Started from the spec (he flagged he hadn't understood the question — good instinct): grid of chars + a word, trace through **4-adjacent** cells, each used once. Taught grid DFS as the 4th backtracking shape: **mark the cell `"#"` in place → recurse 4 directions → un-mark (the backtrack)**; bounds-check base case runs before indexing. Great questions: *when is the un-mark done* (after all 4 calls), *how do we know it's off-grid* (the `<0 / >=len` base case), *do I binary-search the start* (no — double for-loop; wrong starts bail on first-letter mismatch = the `r·c` factor), *space O(L)?* (yes — call-stack depth = one frame per matched letter; in-place mark means no `visited` set). **Structure correct cold** (base cases, mark/un-mark pair, complexity). Two nudges, both **M-027 flavor**: (1) neighbor line came out `down,up,right,right` — dropped **left**, fixed to `dfs(row,col-1,i+1)`; (2) `dfs(0,0,0)` inside the `r`/`c` loop launched every search from top-left — fixed to `dfs(r,c,0)`. **Time O(r·c·4^L), space O(L)** — both stated correctly. **Grid DFS = the bridge into graphs** (implicit graph, neighbors = adjacent cells).
**Theme — the fragility has fully migrated to the newest pattern.** Every previously-named reset-cause (self./ownership, curr3, guard, BFS space, box, enqueue direction, and now the #1046 un-negate) was correct first-draft; the 3 resets are all backtracking, <1 week old. That's the drill working: he doesn't miss the same thing twice.
**Blockers:** **B-7 (self./ownership) CLEARED ✅** (2nd clean session → standing habit) — **no drill-now blocker; all of B-1…B-7 clear.** **M-027 — the one *repeat* (#1046 un-negate) CLOSED**; still watch as the through-line (the two #79 nudges were this flavor). New watch: backtracking fragility (young pattern, not a blocker). New pattern shape: **grid DFS** added to `backtracking.md` (#79). **Patterns learned: 12.** Pace: 26 days to Aug 20; Jul 26 = rest. Next **Day 31 — new material + drain the backtracking 1d cluster.**


## Day 31 — 2026-07-26 — Backtracking drain (3/3) + NEW PATTERN: Intervals (#57, #56)  *(rest Sunday converted to a working session)*
**Why this session existed:** Days 31–53 were **2 sessions short** of fitting before Aug 20, so Jul 26 (+ Aug 9, Aug 16) became working days. Working today recovered one.
**Block 1 — the backtracking drain: 3/3, all first-draft correct from a blank screen, 12 minutes total.** **#90 ✅ 4:30 · #46 ✅ 4:28 · #78 ✅ 2:57** — all → 3d. [INSIGHT] **The Day-30 "backtracking fragility" watch is answered: the pattern was young, not broken.** #90's while-loop placement (after the pop, guarding the *skip* branch) correct; #46's `used`-set-not-index reasoning derived on the second attempt; #78 recognised as #90 minus the while loop. **He dropped `self.` on #46 unprompted, same session as the correction** — fastest turnaround yet. Nice touch on #46: `popped = combo.pop()` then `seen.remove(popped)` can't drift out of sync (better than the usual `seen.remove(nums[n])`). Misses were verbal, not code: omitted `nums.sort()` when describing #90 (found it himself on a `[2,1,2]` test), then missed the sort *again* going #90 → #78.
**Block 2 — pre-teach ran well over.** [STRUGGLE] Had to rebuild `sort(key=)`/`lambda` from the ground up — he asked *"doesn't `data.sort()` just sort it? why is there anything in the brackets?"*, so it went back to "a function is a value," then `def` as a key, then `lambda` as pure typing economy. The unlock was the `words` example: alphabetical vs shortest-first are two valid orderings of one list, and plain `.sort()` can only give the default. Then iterable unpacking — his good question *"doesn't unpacking only work for tuples?"* (no: any iterable; LeetCode hands you **lists**). All checks correct including the trap where a second sort runs on the first's result.
**Block 3 — NEW PATTERN: Intervals.** [INSIGHT] **The primitive, derived by counting the FAILURE cases instead of the success cases:** his first attempt was four strict inequalities; he test-diagnosed its two holes himself (`[1,3]`vs`[3,5]`, `[1,3]`vs`[1,3]`) and reasoned out that **touching must merge**. Patched to `<=` — **verified correct**, four clauses. Then, with a text number-line, got to **`overlap = (b >= c) and (d >= a)`** — two comparisons, because two segments miss only if one is entirely left of the other. *"I reach at least as far as you start, and you reach at least as far as I start."* **#57 Insert Interval ✅** — derived the three buckets (before/overlap/after) from a trace table, 4/4 rows right including `newInterval`-before-the-current-interval (the step most people reverse), and worked out unaided that **one** post-loop guard suffices. First draft had a real bug — `isMerged` was set but never *gated* the branch, so `newInterval` re-appended once per remaining interval; **he found it from the test case `[[1,2],[12,16],[20,25]]`**. Kept his flag-and-gate fix over the early-return version (avoids `enumerate`+slicing). **#56 Merge Intervals ✅ 26:24** — [STRUGGLE] the sticking point was **representation**, three rounds of "compare against the previous interval" before the counter-example `[[1,10],[2,3],[4,5]]` showed it must be **`answer[-1]`**, the interval that's been growing. Taught negative indexing + the unlock that **appending doesn't finalise** — you reach back and stretch the last one. Used both pre-taught tools unprompted.
**Complexity — one real error, and it's the lesson of #56.** Stated `O(1)` space. **A sort costs space as well as time: Timsort is `O(n)` auxiliary.** Never pair `O(n log n)` time with `O(1)` space. Also: he misread #56 as having a sorted guarantee (it has none) — **partly my example's fault**, since the one I gave happened to be sorted by start. The #57-vs-#56 contrast *is* the pattern: both promises vs neither.
**Theme — the gap is verification, not comprehension.** [NEEDS_RECALL] Two new behavioural watches, same root: **M-029** vocabulary looseness (called the `while` loop "a guard before unchoose backtrack," then "a guard with a while loop," then "the if statement guard" — code right every time, language wrong; also garbled the #56 sweep narration) and **M-030** confirm-seeking instead of self-testing (*"am I right about this?"* ×4 — the four-clause condition was 15 seconds of testing away from settled). `GOALS.md` flags that interviewers probe *"what does this line do — remove it, what breaks?"*, and in a room the only verification available is the one he runs himself.
**He asked directly whether needing this much guidance was bad.** Honest answer given: Block 1 was gate-level; Block 3 was first contact with a pattern he met 40 minutes earlier, and the two aren't comparable. The `lambda` gap was a **Python** hole (6 weeks from beginner — expected). He produced a **correct** overlap condition unaided; guidance got him the shorter one. The genuine, transferable gap was the complement move.
**Blockers:** none — all of B-1…B-7 clear, no drill-now. **Backtracking fragility watch CLEARED** (3/3). **M-027** still the through-line (#57's un-gated flag and #56's `pop`+`append` are both two-sites-for-one-fact). New watchlist entries **M-029, M-030, M-031, M-032**. **Patterns learned: 13** (Intervals added, mastery 2/5). Pace: **25 days to Aug 20**, buffer still 1 day (Aug 19). Next **Day 32 (Mon Jul 27) — #79/#39/#57/#56 at 1d, #146 as the first ✍️ one-draft, + Intervals #435/#252.**


## Day 32 — 2026-07-27 — Reviews 3/6 + Intervals cont.: #435 Non-overlapping (greedy), #252 Meeting Rooms
**[FEEDBACK] Coaching change — COACHING rule 12.** Opened by dumping the entire day's plan; he stopped it: *"Don't just dump. You are supposed to be my teacher. Let's go with it one by one."* Logged same session. **The run-sheet is for pre-session setup, not the opening message** — one thing, then wait; he should only see what he's working on now. Dumping the plan is a form of spoon-feeding.
**Warm-up:** didn't recognise the *name* "overlap condition", but with the question made concrete produced `c <= b and a <= d` cold. Label missing, knowledge intact — M-029 shape.
**Block 1 — 3/6.** **#56 ✅ 7:20** (from 26:24) — [INSIGHT] both Day-31 corrections applied unprompted: `answer[-1] = [...]` as one site, and `[a, ...]` not `min(a,c)` **because he understood the sort guarantees the left edge never moves** (a question he couldn't answer yesterday). **#704 ✅ · #33 ✅ → 21d**, both clean 30-sec recalls; #33 had the real insight (*one half is always sorted; find which, then test membership*). **#79 ❌ 10:06** — both Day-30 nudges HELD (all four directions; `dfs(r,c,0)`) and the base-case ORDER was right, but he **peeked at notes for the mark/un-mark idiom — and volunteered it unprompted.** [STRUGGLE] Taught the binding that should retire it: **`board[r][c]="#"` … `= temp` IS choose/un-choose** — the same skeleton as `path.append`/`path.pop`, with the board as the path. **#39 ❌ 9:33** — **prune written backwards**: `target > total` instead of `total > target`, true at the root, so `backtrack(0,0)` returned instantly and `res` came back empty. Same family as B-6 (knows the two operands, reverses them). New check: *"this prune kills the branch when ___"* in English first, then read the code. **#57 ❌ 9:57** — **M-026, the post-loop terminal append missing**, on the exact problem whose own comment said *"make sure to have the isInserted boolean."* He declared it, set it, checked it in-loop, never used it for its one purpose. **The scan didn't fail — it didn't run; "Terminal line written?" is item two on it.**
⚠️ **30-min box enforced** (36:56 of solve time) — **#146 dated to Jul 31**, not rolled. Reason beyond the rule: his first one-draft deserves a clean run, not the tail of an overrun block after three failures.
**Block 2 — NEW: #435 Non-overlapping Intervals + #252 Meeting Rooms.** [INSIGHT] Two Day-31 lessons applied unprompted: **read the spec not the example** (touching is NOT an overlap here → strict `>`), and **`O(n)` space because of the sort** (M-032). Derived the greedy criterion from a worked case: *remove the interval that ends later* — and, importantly, rejected his own second reason (*"it overlaps with the rest"*) as **a consequence, not a criterion**: sweeping left-to-right you can't see the future, but **"ends later" is a LOCAL check that predicts the global property.** Asked about `alist.pop(i)`; flagged the `O(n²)` and mutate-while-iterating hazards, and he got there himself: *"Oh I don't even need to get rid of them, I just have to keep a count."* **🔴 The reference bug — and MY error.** His first version compared physically-adjacent pairs; it fails on `[[1,3],[2,4],[3,5]]` (gives 2, should be 1) because `[2,4]` is used as a comparison partner and then deleted. **[FEEDBACK] I had told him the backward sweep was sound after checking two examples with a plausible-but-insufficient argument — exactly the non-verification I'd spent two days correcting in him. Owned it on the spot.** The fix is a thing he already owns from #56: **compare against the last one you KEPT, not the one physically next to you.** He flipped to a forward sweep unprompted and got it right. **Pattern named: greedy / interval scheduling** — *sort so best-is-first, sweep once, one variable for the last commitment, never look back*; connected back to #11 (move the shorter wall) and #121 (running min) as greedies he'd already done unlabelled. Also drew the **greedy vs sliding-window** line at his request: *does anything ever leave?* — a window is state about a range, a greedy is state about a commitment. **#252** built quickly on the same machinery; good reasoning on why a `ref` is worth keeping even where it isn't needed (avoids an index bound — sound, given B-2).
**[INSIGHT] The finding of the day: all four full solves were FIRST retrievals. Three failed. The one that passed — #56 — is the one he struggled with hardest yesterday** (26:24, three explanations). #57 went smoothly yesterday and today its terminal line was gone. **Desirable difficulty, visible in his own data.** Tell him this again on a session that feels bad.
**Every failure was completeness, not comprehension** — missing terminal line, reversed comparison, un-recalled idiom. **Improving:** tested his own approach when asked rather than seeking confirmation (M-030), self-reported the notes peek, pushed back on the dumping. **Still slipping:** method names four sessions running (`exist`/`merge`/`insert`/`eraseOverlapIntervals` all renamed), *"the mid value… that is the index"* (B-5), and no stopwatch on new problems for a third session.
**Blockers:** none escalated. **M-026 fired** (watch). **M-029/M-030 continue** (2nd session). Backtracking fragility **reopened** — solid on rehearsed problems, fragile on first retrieval. New: **M-033** (prune written backwards), **M-034** (scan said but not run), **M-035** (coach: validated an approach on two cases). **Patterns learned: 13** (Intervals 3/5; greedy previewed). Pace: **24 days to Aug 20**, buffer 1 day. Next **Day 33 (Tue Jul 28) — Graphs #200/#133, Block 2 FIRST.**

<!-- Append Day 31+ below. Format: ## Day N — YYYY-MM-DD — topic / problems / [tags] -->

## Day 33 — 2026-07-28 — NEW PATTERN: Graphs (#200 Number of Islands, #133 Clone Graph) + Block 1 **5/5**
**Session shape:** Graphs is heavy ⇒ **Block 2 ran FIRST** while fresh (COACHING rule 11 / weight-balancing), Block 1 after, under the hard 30-min box.
**[FEEDBACK] Coaching change — COACHING rule 3 refined.** I demanded time/space before he had an approach on a brand-new problem; he pushed back — *"I can't really give you a time and space complexity until I actually have an idea about the way I want to go about the solution first"* — and he was right. **Now standing: reviews = complexity FIRST (pure recall, that's the drill); new material = approach → complexity → code. Invariant either way: complexity before any code.**
**Block 2 — NEW: Graphs.** Framed as **implicit vs explicit** — the traversal is identical, the only variable is where neighbours come from (computed from `(r±1,c)` vs stored in `node.neighbors`). He already owned grid DFS from #79, so graphs added a neighbour *source*, not a new algorithm.
**#200 Number of Islands ✅ 44:14.** [INSIGHT] Approach unprompted and exactly right: *"use the word-search DFS but instead of temporarily marking the node, permanently mark it."* And the complexity reasoning was the best moment of the block — *"the reason 4^L is not considered is because you can only work with a node once."* **Generalised for him: backtracking explores PATHS (exponential); graph traversal visits NODES (linear) — the un-mark is the entire difference.** One real confusion, resolved: **where `count += 1` goes** — not inside `dfs` (which sinks a whole island) but beside the call in the outer double loop, gated by `if grid[r][c] == "1"`. `O(m·n)` / `O(m·n)` both stated correctly.
**#133 Clone Graph ✅ 45:28, after two bugs.** [STRUGGLE] **(1) `Node(node.val, node.neighbors)`** — handed the clone the ORIGINAL's neighbour list, so it isn't a deep copy at all; fixed to `Node(node.val, [])` + `adict[node].neighbors.append(dfs(neighbor))`. **Named as container-vs-contents (M-021) at object scale: `node.neighbors` is a handle to the originals; he wanted the things they map to.** Logged as **M-036**. **(2) `node.neighbor` — missing the `s`**, with `self.neighbors` written three lines above in the class definition. **The algorithm was correct at submission two; the typo cost the third submission.** Taught the two ideas worth keeping: `adict` is **the mapping, not a visited set** (one structure answering *seen?* and *what's its clone?*), and **register the shell BEFORE recursing** or a cycle never terminates.
**`O(V + E)` had to be animated — and he was right to insist.** Asked three times for a visualisation rather than nodding along (*"you gotta visualise it, I have no clue what you're talking about with the edges"*). Built **`viz/graph-edges-and-traversal.html`** (Part 1: one edge → two entries; Part 2: a clone traversal counting V and E separately), committed to the repo and to his Day 33 folder. [INSIGHT] **He then produced the key fact himself: *"if there are E edges, it will always be 2E entries."*** Undirected stores each edge twice; directed once. **Why `V +` exists: an isolated node still costs work — V pays for existing, E pays for connecting.** Also drew the greedy-vs-sliding-window line again at his prompt (*"so this is not necessarily a sliding window"* — correct).
**Block 1 — 🟢 5/5 in 26:55, all FULL solves, inside the box with 3 min spare.** **#79 ✅ 6:22 · #39 ✅ 7:24 · #57 ✅ 6:06 · #435 ✅ 4:50 · #252 ✅ 2:13 — all → 3d.** Every solve verified by executing his exact code against 6–10 cases including edge cases. [INSIGHT] **The finding of the day: yesterday he failed three of four full solves; today he passed five of five, and every one was a fix to the exact thing that broke.** #79's mark/un-mark idiom written cold (peeked at notes for it yesterday); **#39's prune correct** (`total > target`, reversed yesterday — **M-033 held**); **#57's post-loop terminal append present** (missing yesterday — **M-026 held**); **#435 comparing against the interval it KEPT**, not the physically adjacent one. **Every Day-32 named cause held first-draft. That is the ladder doing exactly what it's for.**
**#252 — and what he got away with.** Wrote the pattern as a comment *before* the body (*"similar to nonOverlapping, sort by x[1], have a temp…"*) — retrieval then writing, which is why it took 2:13. Corrected the takeaway: **in #435 sort-by-END is load-bearing; in #252 it is not** — sort-by-start works equally well (verified vs brute force on 300k random cases under LeetCode's `start < end` guarantee). **The reason: #435 SKIPS intervals so `temp` diverges from "the previous element"; #252 returns on the first conflict so nothing is ever skipped and the trap can't fire.** Filed as: *the sort key exists to make the greedy choice come first — if you never skip, there's no greedy choice to protect.*
**🔴 The two honest problems.** **(1) Ninety minutes for two new problems** (44:14 + 45:28). Both built correctly and the material was brand new — but **the readiness gate is minutes on unseen problems**, and a FAANG medium is ~20–25. This is now the number to watch, not pass/fail. **(2) `node.neighbor` is the SIXTH session of names being slightly wrong** (`exist`→`wordSearch`, `merge`→`mergeInterval`, `insert`→`insertInterval`, `eraseOverlapIntervals`, *"the mid value… that is the index"*, now `neighbor`). **M-029 hits 3 sessions ⇒ ESCALATED to blocker B-8 per the standing ≥3 rule.** Also installed, from Day 34: **a full solve handed over without being executed is a FAIL on the spot** — no partial credit for "the algorithm was right."
**Improving:** stopwatch ran on new problems for the first time in four sessions (the standing miss is closed); demanded a real explanation instead of nodding; stated `O(n log n)` **and** `O(n)` space on #252 unprompted (M-032, the Day-31 error, now automatic).
**Blockers:** **B-8 OPENED (naming precision — drill now).** M-036 new (deep copy built from originals). **M-026, M-033, M-034 all HELD** — no Day-32 cause recurred. **Patterns learned: 14** (Graphs added, 2/5; Intervals held at 3/5 — four clean problems, but all four are the ones he was taught). Pace: **23 days to Aug 20**, buffer still 1 day (Aug 19). Next **Day 34 (Wed Jul 29) — Graphs cont. #994 Rotting Oranges / #207 Course Schedule, Block 2 FIRST; warm-up cold-tests `O(V + E)` on fresh numbers.**

## Day 34 — 2026-07-30 — Graphs cont. (#994 Rotting Oranges, #207 Course Schedule) + Block 1 3/6
**Warm-up ✅ — `O(V + E)` cold, no notes.** 5 nodes / 6 undirected edges → **12 entries**, reason correct; and *"you are doing a job at each vertex — it's not E work for each of V vertices."* **The thing that needed animating yesterday came back cold in under a minute** — banking it worked. *(B-8: "entry walks" fuses two concepts — an **entry** sits in memory, a **walk** traverses one; also vertex/vertices.)*
**#994 Rotting Oranges ✅ 1:12:18 total, 22:24 unaided.** [STRUGGLE] Opened with DFS-longest-depth. **He spotted the multi-source problem himself** (*"if there are two it'll be faster, I don't know how to go about that"*) — good instinct. But the plan was broken with ONE source too, and finding that was the lesson: on `[[2,1,1],[1,1,1]]` his DFS reports **5**, true answer **3**. [INSIGHT] **DFS depth is the length of the path it happened to take, not the shortest distance — and the permanent mark freezes the wrong number.** Time IS a shortest distance ⇒ DFS cannot compute it. Framed why `#200` was fine: *"are these connected?"* doesn't care about the route; *"how far?"* does. He named BFS/level-order and `deque` unprompted, and derived **multi-source seeding** himself from *"what's in the deque before the first popleft?"* — all sources at level 0 ⇒ nearest source wins automatically, no comparison. **Off-by-one taught and measured:** bare `while q` counts a final level that rots nothing → 4 not 3; his own `minute - 1` fix verified correct on all 81 2×2 grids + 20k random **except an all-`0` grid**; he chose `while q and fresh > 0` instead. **Python misconception corrected: `range(len(q))` is evaluated ONCE** (he believed it re-evaluates and grows) — `while i < len(q)` does re-check. Complexity `O(m·n)`/`O(m·n)`; he first said `O(r)` space, then `O(m+n)` (correct for a SINGLE source — measured peak deque 10 corner / 19 centre), then got `O(m·n)` off the all-rotten case. **BFS space = widest level, and in multi-source BFS the seeding IS a level** (M-005 new face). Code verified: all 2,916 six-cell grids + 30,000 random, zero mismatches.
**🔴 Two assertion failures, and they are the day's real story.** Asked to trace HIS dfs, he simulated the **correct rotting process** instead. Asked what his BFS counter ends at, he said *"it does give 3"* — it gives **4**. **M-034 in its purest form: checking what the ANSWER is and assuming the code agrees.** He pushed back fairly (*"how am I supposed to run it, I have no code"*) — conceded that half; held the real point: **the failure was asserting a result he hadn't derived. *"I haven't checked"* costs nothing.** He had hand-traced the DFS correctly ten minutes earlier, so the capability was there.
**#207 Course Schedule ✅ 43:52** (after one bug). Needed the pair convention made concrete before he could start — fair, `[a,b]` = "take b before a" is genuinely confusing. Named **cycle** as the property. Corrected: no sort, no "find the start" (try every node — the cycle may not be reachable from 0), and not level-order. [INSIGHT] **Derived the core distinction himself** after one killed guess (*"3 is a leaf"* — killed by adding `3→4`): ***"the first dfs(0) has not returned yet, whereas dfs(3) had."*** ⇒ **a cycle is "I've seen this node AND I'M STILL INSIDE IT."** Then named `path`'s add/remove shape as **backtracking** unprompted. **Taught the unifying idea of the week: graph traversal and backtracking are the permanent mark and the temporary mark** — un-mark ⇒ explore paths ⇒ exponential (#79); never un-mark ⇒ visit nodes ⇒ linear (#200); **#207 needs one of each.** He asked *"why do we even need visited?"* — excellent question; answered with measurement: **55 nodes / 72 edges → 127 calls with `visited`, 4,194,163 without**, same answer. Also asked *"why not interval overlap?"* — answered with the general tool: **test a method against a symmetry the answer must respect** (relabel the courses and the answer can't change ⇒ any position-reading method is dead), which kills intervals, sliding window and binary search on all graph problems at once. **🔴 Bug: `dfs(child)` with the return discarded** — every True case passed, every False case failed. **M-001, the oldest entry in the database**, and it was item three of four I'd listed before he wrote. He fixed it from the description; verified on all 6 cases, **all 4,096 directed graphs on 4 nodes**, self-loops, duplicate edges, 20k random — zero mismatches.
**🟢 [INSIGHT] "I pretty much just copied from whatever you have given me."** Saying that was worth more than the working code — built **`viz/dfs-cycle-detection-stepper.html`** (call stack + `path` + `visited` + the real code, stepped line by line, two examples), after which he produced the model unaided. Corrected one phrase: *"every way through it is fine"* is **path**-language (exponential); say *"nothing reachable from this node leads back into the path I'm currently on."*
**Block 1 — 3/6.** **#200 ✅ 7:17 → 3d** (all 512 3×3 grids + 20k random; pattern line before the body again). **#90 ✅ 6:15 first draft, never executed → 7d** (7 named + 3,000 random with duplicates; the dedup `while` correctly after the pop, guarding the skip branch — the line he narrated wrongly on Day 31). **#155 ✅ → 21d** (the `<=` for equal minima AND the empty-guard). **#133 ❌ reset** — [STRUGGLE] **yesterday's TWO bugs, identical**: `Node(node.val, node.neighbors)` (clone wired to the originals — measured 5 reachable nodes instead of 4) and the discarded `dfs(neighbor)`; plus the missing `None` guard (B-4). **#46 ❌ reset** — `if combo == len(nums)` compares a **list** to an **int**, always False, `res` returns `[]` on every input (**B-5**); the rest of the draft was correct. **#74 ❌ reset** — `while left < right` with `right = mid - 1` never examines the final cell; **4 of 6 cases wrong** incl. `[[1]]` target 1. Also stated `O(log n)` where it is `O(log(m·n))`. **#78 not reached — dated Jul 31**, not rolled.
**🔴 THE DIAGNOSIS, and it is the narrowest this project has produced:** `dfs(child)` discarded · `dfs(neighbor)` discarded · `combo == len(nums)` · `while left < right`. **Four problems, four failures, ONE disease — the exact identity of the value in front of you. Not one was a comprehension gap**, and two of the four are the *same* recursion mistake within two hours. **New standing drill: before writing any recursive function, say *"the callee hands me back ___, and I catch it at ___."*** [INSIGHT] **And the mechanism behind #133: it is the one problem he never wrote correctly himself — a correction you're HANDED decays far faster than one you DERIVE.** Everything he fought for today survived; the thing he was told did not.
**Decisions taken today (all standing, all his call).** (1) **2 new/day stays, long sessions accepted** — chosen over shortening first-contact days; consequence: Block 2 first on those days, Block 1 never dropped. (2) **🎯 Unaided timed mediums on the interleave days — Aug 1, Aug 8, Aug 14** — HIS proposal, better than mine (zero throughput cost). Unseen problem from a learned pattern, 35-min hard cap, **coach silent**, narrate throughout, self-score on 4 dimensions. **This closes the only readiness gate genuinely off-track: `GOALS.md` requires 4-of-5 unseen mediums in ≤35 min unaided, and we have ZERO unaided data points in 34 sessions.** (3) **One-draft cap 4 min → 6 min when a nested recursive helper is needed** — he measured it rather than complaining. (4) **[FEEDBACK] I now supply the exact signature on every review prompt** — he writes in IDLE from scratch and never sees LeetCode's stub, so three "invented method name" catches were MINE, not his; **B-8 rescoped** to names visible in front of him (`node.neighbor` with `self.neighbors` three lines above) and construct-naming in narration.
**Blockers:** **B-8 rescoped** (naming precision — 1 valid instance today, not 3). **B-9 OPENED — the recursive return channel** (M-001 fired twice in one session, on `#207` and `#133`). M-036 recurrence 2. **B-5 fired** (#46). Binary-search template mixing (#74) added to `binary-search.md`. **Patterns learned: 14** (Graphs 2/5 — both problems correct but 72 and 44 min; the reps decide the number). Pace: **21 days to Aug 20** — 🔴 **Jul 29 was skipped, so the Aug 19 buffer is SPENT: 19 sessions into 19 available days.** Recovery = one triple-new day, or work Sun Aug 2. Review drain extended to **Aug 9** (3 resets + the 6-min recursive rule). Next **Day 35 (Fri Jul 31) — #210 Course Schedule II / #323 Connected Components, Block 2 FIRST; Block 1 = 5 full solves, 30 min, nothing else.**

## Day 35 — 2026-07-31 — Graphs cont. (#210 Course Schedule II, #323 Connected Components) + Block 1 2/5
**Warm-up — the two sets, cold.** Both definitions right, but asked *what breaks* he restated the definitions; the follow-up got it: **delete `visited` → right answer, 4.19M calls instead of 127; delete `path` → wrong answer, fast.** [INSIGHT] **`visited` is a speed guarantee, `path` is a correctness guarantee** — know which of your structures is which.
**#210 Course Schedule II ✅ 17:21.** [INSIGHT] **Derived it himself** — *"when we add to visited, we add it to a list and then return the reverse"*; his earlier instinct (*"right before unchoosing"*) was the same moment. **#207 plus two lines.** Sharpened the *why*: **every node that finishes before X is reachable FROM X (depends on X), so X must precede them all** — finish-order lists dependents first, reversing fixes it. (He had the stack picture right and one label backwards: the LAST node to finish is at the BOTTOM of the stack and lands FIRST after the reverse.) **🔴 Bug: `return answer` placed INSIDE the outer loop** → the loop runs exactly once; **2,103 of 5,000 random inputs failed**, every failure a multi-component graph. Survived all my examples because they were single-component. [INSIGHT] **Rule extracted: an early EXIT (`return []`) lives inside the loop; a CONCLUSION (`return answer`) lives after it** — ask what the loop's job is, and here it's only true once the loop ends. Fixed: 11 named + 30,000 random, zero failures.
**#323 Connected Components ✅ 31:14.** **Transferred it from #200 himself** once asked *"how did you know the next 1 was a different island?"* — `seen` replaces the `"#"` flood, counter beside the call. **Two judgment calls made correctly and unprompted: (1) he did NOT bring `path` over** (undirected connectivity has no cycle question — transferring a pattern *without* the parts you don't need is the hard half of pattern recognition); **(2) his `dfs` returns nothing, which is correct** — with B-9 live, made it explicit that the drill is *know what the callee hands back*, not *always catch a return*; **mixed intent is the bug**. Undirected ⇒ two appends per edge into two different lists (he asked whether a guard was needed — no; that IS the 2E from Day 33). 🔑 **Space discussion worth more than the problem:** he said `O(V)`; it's `O(V+E)`. **Rule: count the space YOU allocate, not the space the input arrived in** — #133 was `O(V)` because the neighbour lists came *inside* the input; here he builds the adjacency. Then he asked *"isn't a dict's space just its number of keys?"* → `{0: [a million ints]}` has one key and is not `O(1)`. **A container's space is its overhead PLUS everything it holds** (M-005's third costume, after BFS queues and Timsort scratch). Verified: 8 named + **every simple graph on ≤4 nodes exhaustively** + 20,000 random + a 900-node chain.
**Block 2 total: 48 minutes for two new problems, against 116 yesterday.** Same pattern family, second day — consolidation visible on the clock.
**Block 1 — 2/5, 26:12.** **#74 ✅ 4:26 → 3d** (`while left <= right` paired correctly with `right = mid - 1` — **the exact thing that reset it yesterday, held first-draft**; 10 named + 30,000 random matrices). **#207 ✅ 5:38 → 3d** (**`if not dfs(child): return False` first-draft — B-9's exact failure from yesterday, caught cold**; all 4,096 directed graphs on 4 nodes + 20,000 random). **#133 ❌ reset ×3** — `for neighbor in adict[node].neighbors` walked the CLONE's empty list; returns one isolated node (measured: 1 reachable, expected 4). **He self-reported a notes peek — second time, unprompted.** **#46 ❌ reset ×2** — `backtrack()` defined and **never called**; `[]` on every input. Base case fixed from yesterday; add one call line and every permutation is correct. **#994 ❌ reset** — `for r in range(grid)` → **TypeError, the program doesn't start**; with `len()` added and nothing else changed it passes every 6-cell grid and 20,000 random. **This is M-003/B-2, cleared Day 18, reopened today.**
**🔴 THE FINDING: all three failures were correct algorithms that never executed.** `#133` walked the wrong object's list · `#46` never invoked its own function · `#994` crashed on line one. **Not one is an algorithm; all three die on the FIRST example.** [INSIGHT] **The gap between his solve rate and his pass rate is one execution.** Yesterday: same story, different lines. **And the one problem where he applied the B-9 drill (#207) is the one that stopped failing.**
**🔴 #133 — three sessions, one ambiguity.** Day 33 `Node(node.val, node.neighbors)`, Day 34 identical, Day 35 `for neighbor in adict[node].neighbors`. **Every failure is the same question: WHICH `neighbors` list.** And this time **he had his notes open and still got it wrong**, so it is not recall — when two objects share an attribute name he has no firm grip on which is which. **Prescription: name the clone.** `clone = Node(node.val, [])` … `for neighbor in node.neighbors: clone.neighbors.append(dfs(neighbor))`. **When two things are easy to confuse, stop relying on care and change the names** — same move as `answer[-1]` replacing `pop()`+`append()` on #56. **M-036 hits recurrence 3 ⇒ B-5 REOPENED as a drill-now blocker.**
**Blockers:** **B-5 REOPENED (container vs contents / two objects sharing an attribute name)** — M-036 ×3 plus M-037. **B-2 reopened as a watch** (`range(len(x))`, clean since Day 18). **B-9 held on #207** (1 clean). B-8 quiet. New: **M-039** (function defined but never invoked). **Patterns learned: 14** (**Graphs 2/5 → 3/5** — six problems, and Block 2's time halved). Pace: **20 days to Aug 20**; Days 36–53 = 18 sessions into 18 available days — **still zero buffer, recovery owed and undated.** Next **Day 36 (Sat Aug 1) — 🎯 MOCK #1** (unseen medium, 35-min cap, coach silent, self-score 4 dimensions), then Block 1: **#133 · #46 · #994** (resets) → **#210 · #323** (1d).

## Day 36 — 2026-08-01 — 🎯 MOCK #1 (#567 Permutation in String) + Block 1 **5/5**
**🎯 MOCK #1 — the first unaided data point in 36 sessions. 19:38, fifteen minutes UNDER the 35-minute cap, correct code.** I had told him in advance to expect over 35; every prior new problem took 44–72 min *with* me talking. [INSIGHT] **He named the pattern in his first sentence** (*"this is very clearly a sliding window problem"*) — `GOALS.md` sets that gate at 2–3 minutes. Verified 9 named + 20,000 random string pairs vs brute force, zero mismatches. He also **tested it, found a real bug himself** (`while right > len(s2)`, condition inverted), fixed it, and reported it unprompted.
**Scores — his 4.5 / 2.5 / 1.5 / 3 against mine 4 / 2.5 / 2 / 3.** [INSIGHT] **His calibration is the quiet result: least confident exactly where he was wrong, most confident exactly where he was right.** Four days ago he asserted *"it does give 3"* about an untraced output. Real change.
**🔴 The mock's finding — he identified the optimal solution and rejected it.** *"If I were to take a more optimal route, I would have had to complicate it more by using a dictionary like the valid anagram question."* **That instinct is the thing to fix**: in a room, "I know a better approach but it's fiddlier" is not a reason to skip it, it IS the answer, and the interviewer's next words are *"can you do better?"*. Logged as **M-040**. Scripted the sentence he should say instead.
**🔴 And it would TLE.** He stated `O(n log n)`; it is `O((n−m)·m log m)`, worst case `O(n² log n)`. Measured at the constraint ceiling (len ≤ 10⁴): **his version 5.67 s · with `key1` hoisted out of the loop 2.80 s · the `O(n)` rolling count 0.001 s.** Note the middle number — **`key1 = "".join(sorted(s1))` is loop-invariant and recomputed every pass; hoisting one line halves the runtime.** **M-006 third occurrence** (Group Anagrams Day 3, Longest Substring Day 8) ⇒ **ESCALATED to B-10: price the body of every loop.** He derived the `O(n)` fix after one question (*"the window slides one position — how many characters actually change?"* → **two**), so `#567` enters the queue at 1d to be written properly rather than eating Block 1 time.
**Block 1 — 🟢 5/5 in 52:53. Three resets and two first retrievals, all passed.** **#133 ✅ 6:49 → 3d** (verified against 2,000 random connected graphs with object-identity checks) · **#46 ✅ 6:48 → 3d** (3,000 random) · **#994 ✅ 17:40 → 3d** (every 6-cell grid + 30,000 random) · **#210 ✅ 11:40 → 3d** (30,000 random with a validity checker; `return revAnswer[::-1]` correctly OUTSIDE the loop — yesterday's exact bug) · **#323 ✅ 9:56 → 3d** (every simple graph on ≤4 nodes + 30,000 random + a 900-deep chain; **31:14 yesterday with a prompt, 9:56 today cold**).
**🔑 THE FINDING OF THE SPRINT SO FAR.** Yesterday these same five went **2/5**, and all three failures were *correct algorithms that never executed*. Today: **5/5**. [INSIGHT] **His understanding did not change overnight — he ran the code before sending it. That is the entire delta, now measured rather than asserted, on the same five problems one day apart.** **#133 is the proof: its first draft contained the IDENTICAL Day-35 bug (`for neighbor in clone.neighbors`) plus `.children` for `.neighbors` — he ran it, both surfaced, he fixed them and told me.** Two sessions ago that arrives broken and costs a reset.
**🟢 The scheduling call was his, and it was right.** At the 30-minute mark with two items left he asked to take a break and finish rather than push to Monday, **because he spotted that Monday carries three new DP problems and two more full solves would stack** (arithmetic: Monday 8 items/27.5 min + 12 min = 10 items/39.5 min, 30% over, on the least absorbent day). He took a real break and came back to a defined twelve minutes. **That is managing the box, not blowing it — and it is the judgment the box exists to teach.**
**Blockers:** **✅ B-9 (recursive return channel) CLEARED** — 2 clean sessions; the return was caught correctly on all five solves. **B-5 — 1 clean session** (#133 passed; needs one more, and he still wrote `graph[node].neighbors` instead of using the name `clone`). **B-8 — a `.children`-for-`.neighbors` slip on #133's first draft, self-caught by running; not cleared.** **⛔ B-10 OPENED (price the loop body, M-006 ×3).** New: **M-040** (identified the optimal approach and rejected it as too complicated). **Patterns learned: 14** (Graphs 3/5 — six problems, all six now passing; Sliding Window 3/5 with #567 added). Pace: **19 days to Aug 20**; Days 37–53 = 17 sessions into 17 available days — **still zero buffer, recovery owed and undated.** **Review drain extended to Aug 11** — five 3d passes plus the mock item all land in a week that was already full. Next **Day 37 (Mon Aug 3) — 1-D DP first contact: #70 / #746 / #198, three new, Block 2 FIRST.**
